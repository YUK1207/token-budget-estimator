interface:
  display_name: "Token Budget Estimator"
  short_description: "Predict agent task token usage before execution."
  default_prompt: "Use $token-budget-estimator to estimate the token budget and risk before executing this task."
policy:
  allow_implicit_invocation: true
